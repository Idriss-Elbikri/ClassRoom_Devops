trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

variables:
  artifactName: 'drop'
  warFileName: 'ClassRoom.war'

steps:
# Étape 0 : Installer Java 11
- script: |
    sudo apt-get update
    sudo apt-get install -y openjdk-11-jdk
    java -version
  displayName: '☕ Installer Java 11'

# Étape 1 : Build avec Maven
- task: Maven@3
  displayName: '🔧 Build avec Maven'
  inputs:
    mavenPomFile: 'pom.xml'
    goals: 'clean package'
    options: '-DskipTests=true'

# Étape 2 : Copier le fichier .war
- task: CopyFiles@2
  displayName: '📦 Copier le .war'
  inputs:
    contents: '**/target/*.war'
    targetFolder: '$(Build.ArtifactStagingDirectory)'

# Étape 3 : Publier l'artefact .war pour la Release
- task: PublishBuildArtifacts@1
  displayName: '📤 Publier Artefact .war'
  inputs:
    pathToPublish: '$(Build.ArtifactStagingDirectory)'
    artifactName: '$(artifactName)'

# Désactiver les résultats de tests si inutiles
- task: PublishTestResults@2
  condition: false
  displayName: '📋 Publier les résultats des tests (désactivé)'
